---
name: liquidity-reel-production
description: DEFAULT production settings for ALL Liquidity State visual content (reels, carousels, posters, charts). ALWAYS load this skill before designing or animating ANY new content idea for the account — it locks the approved colors, dark poster cover, precise chart markup rules, dense candle counts, sharp box shapes, exact icosahedron logo, Kuwaiti lexicon, and the HTML→Playwright→ffmpeg pipeline with ready scripts. Triggers: ريل, ريلز, كاروسيل, بوست, تصميم, فكرة جديدة, محتوى, انتج, صمم, الماركت ستركجر, any new Liquidity State content idea. These defaults OVERRIDE the generic visual identity in liquidity-state-brand §2 (the account's real templates are light-canvas content + dark cyan poster covers).
---

# Liquidity State — Reel/Carousel Production Defaults

هذي الإعدادات الافتراضية **المعتمدة نهائيًا** من فهد (جلسة 2026-08-01). أي فكرة/محتوى جديد للحساب يبدأ من هنا بدون إعادة نقاش. عند التعارض مع `liquidity-state-brand` §2 (الألوان)، **هذا الملف يفوز** — التمبليتات الحقيقية للحساب فاتحة، مو غامقة.

## 1. نظام الألوان المعتمد (لا تخترع غيره)

### المحتوى (كل المشاهد/السلايدات ما عدا الغلاف)
| دور | قيمة |
|---|---|
| خلفية | radial: `#F7F3EC` → `#F2EEE7` → `#ECE6DB` (كريمي فاتح) |
| عناوين/نص أساسي (INK) | `#0F2E3C` |
| تركواز أساسي / غامق / فاتح | `#2E7D96` / `#1E627A` / `#5AA6BC` |
| شمعة صاعدة / هابطة | `#2E8CA6` تركواز / `#122F3E` كحلي |
| أحمر (حصريًا للهبوط: LL/LH/CHoCH) | `#D24B4B` |
| نص ثانوي / محاور | `#5C6C73` / `#93A2A8` |

### بوستر الغلاف (أول مشهد بالريل فقط — غامق)
- خلفية: radial `#0C2029` → `#08131C` → `#04090F`
- سيان التوهج: `#43D4DC` (glow: `rgba(67,212,220,.55)`)
- العنوان: تدرج معدني `#ffffff → #C4D4DB → #7F97A1` (background-clip:text) + drop-shadow سيان خفيف
- Wordmark: تدرج أبيض→فضي، letter-spacing واسع (7-11px)
- فواصل: خط متدرج سيان + معيّن (rotate 45°) متوهج في الوسط
- اللوقو بالبوستر: drop-shadow سيان 36px

## 2. اللوقو — مجسم عشريني دقيق (إلزامي)

**ممنوع** اللوقو المبسط (5 مضلعات). استخدم `scripts/gem.py` — icosahedron محسوب فعليًا:
- 12 رأس بالنسبة الذهبية φ، 20 وجه مثلثي، back-face culling، تظليل معدني حسب زاوية الضوء `(-0.32, 0.74, 0.6)`
- تدرج الأوجه: `(80,101,113)` → `(242,247,250)`، حواف `#54687a` بعرض 0.32
- الزاوية المعتمدة: `rx = 54°` (رأس للأعلى، سيلويت سداسي)
- جاهز داخل `reel_build.py` كـ `GEM=_gem(math.radians(54.0))`

## 3. قواعد الماركب على الشارت (دقة صفر-عشوائية)

1. **كل خط/نقطة/صندوق مثبّت على سعر OHLC حقيقي** — رأس فتيل أو إغلاق فعلي. مافي مستوى "تقريبي" أبدًا.
   - خط BOS = قمة الفتيل بالضبط، SSL = قاع الفتيل بالضبط، حدود الصندوق = أسعار السوينق الفعلية، رأس سهم الكسر يلامس الخط تمامًا، الزقزاق يوصل أطراف الفتائل.
2. **بدون إطارات نهائيًا** حول نصوص الشارت: نص Tajawal + هالة كريمية (`paint-order:stroke; stroke:#F2EEE7; stroke-width:~6.5`) — دالة `htext()`.
3. **الخطوط الأفقية (rays)**: رفيعة 1.6–1.8px، نهاياتها **دوائر مقبض** (r=5.5، تعبئة كريم، حد بلون الخط) — ستايل TradingView (دالة `hend()`).
4. **صناديق الرينج/التجميع**: تعبئة تركواز `#2E7D96` بشفافية 14–18% + حد 1px `#1E627A`. **ممنوع الرمادي**.
5. **دلالة الألوان**: كحلي INK = مستويات محايدة/هيكل، تركواز = صاعد/كسر صاعد، أحمر فقط للهبوط والانعكاس.
6. وسوم الإنجليزي على الشارت: `BOS`, `SSL`, `Breakout` (مائل), `HH/HL/LH/LL` — نص فقط بدون خلفيات.

## 4. كثافة الشموع

- **الشروحات التعليمية: 20–40 شمعة (الافتراضي 30)** — ممنوع 6–10 شموع. مولّد `gen()` في `reel_build.py`: random-walk على anchors مع فرض نقاط سوينق معلومة الفهرس عشان الوسوم تنطبق بدقة.
- **ممنوع تكرار نفس الشارت بين مشاهد الفيديو** — كل مشهد له بيانات شموعه الخاصة (anchors + seed مختلفة، سيلويت مختلف فعليًا). للدروس متعددة الخطوات على نفس المفهوم: ابنِ سيناريو مستقل لكل خطوة بدالة مصنع (نمط `make_ob()` في `reel_ob_build.py`) بحيث كل وسم يتثبّت على أسعار سيناريوه هو.
- 🔒 **أمر دائم — تنوع شديد بالشموع (واقعية):** أجسام وذيول وأحجام متباينة بقوة مثل الشارتات الحقيقية — توزيع مختلط (~16% دوجي/جسم ضئيل، أغلبها عادي، ~10% شموع اندفاعية كبيرة)، ذيول بعشوائية تربيعية (أغلبها قصيرة وبعضها بن بار طويل)، وعنقدة تقلب (فترات هادئة/نشطة ×0.4–2.0). **ممنوع أحجام وذيول متقاربة/رتيبة.** مطبقة داخل `gen()` في `reel_build.py`، مع سقف ارتفاع زون OB (~1.9 وحدة) في `make_ob()`.
- 🔒 **أمر دائم — ممنوع تكرار الشارتات عبر الفيديوهات**: أي شارت استُخدم في فيديو قديم ممنوع يظهر في فيديو جديد، للأبد. الإنفاذ آلي عبر السجل الدائم `scripts/used_charts.json` + `scripts/chart_registry.py`:
  1. قبل بناء أي فيديو جديد: `assert_fresh_synthetic(seed, anchors)` لكل شارت تخطيطي، و`assert_fresh_real(symbol, tf, start, end)` لنافذة البيانات الحقيقية (نافذة/أداة جديدة لكل فيديو).
  2. بعد اعتماد الفيديو: `register_synthetic(video, charts)` + `register_real(...)` ثم **كمّت السجل وادفعه** — السجل جزء من الريبو عشان المنع يستمر عبر الجلسات.
  3. `python3 chart_registry.py` يعرض كل المستخدم سابقًا. كل الشارتات حتى تاريخه (ريل الستركجر + الدليل + ريل الأوردر بلوك + نافذة EURUSD يومي 06-15→07-31) مبصومة ومحظور إعادة استخدامها.
- 🔒 **أمر دائم — الصفقة/المثال الحقيقي على 20–30+ شمعة:** نافذة المثال الحقيقي تعرض **20–30 شمعة أو أكثر** تغطي القصة كاملة بسياقها (القاع ← الرينج ← السحب ← الزون ← الكسر ← الهدف) — مع بقاء الوضوح شرطًا: الزون سميك والوسوم مقروءة على الجوال، وممنوع نافذة تحشر القصة بزاوية صغيرة.
- **مشهد "السوق الحقيقي": بيانات OHLC حقيقية فقط** (Alpha Vantage `FX_DAILY` أو مصدر معتمد). ممنوع اختراع شموع. القصة المعتمدة: LL → HL → BOS → HH (انعكاس اتجاه) والافتراضي EUR/USD يومي إذا الذهب محجوب.
- **الفريمات الصغيرة (M5/M15/M30):** نقاط intraday في Alpha Vantage وFMP مقفلة (باقات مدفوعة) وشبكة الحاوية تحجب المصادر الحرة — الحل المعتمد: تشغيل `scripts/intraday_scan.py` داخل sandbox_exec (Higgsfield) حيث يجلب Yahoo Finance (`EURUSD=X`، فواصل `5m/15m/30m`، آخر 60 يومًا) ويفحص بنفس كواشف الأنماط الستة ويعيد الصفقات المختارة فقط. النقل يتم بمقاطع base64 مع md5 لكل مقطع (النسخ اليدوي الطويل يتلف). تسميات الوقت **بتوقيت الكويت (UTC+3)**، والشريحة: «EUR/USD 15 دقيقة · التاريخ». محور السعر يستخدم `pstep="auto"` في `chart()` للمدى الضيق.
- 🔒 **أمر دائم — تنويع الأدوات:** ممنوع حصر الأمثلة الحقيقية بـ EUR/USD — نوّع عبر المحتوى بين **أزواج فوركس متعددة، الذهب، الداو جونز، الناسداك**. رموز Yahoo المتحقق منها (2026-08-01، يومي + 15m): أزواج `GBPUSD=X` `USDJPY=X` `GBPJPY=X` `AUDUSD=X` (24/5 كاملة)، الذهب `GC=F`، الداو `YM=F` (فيوتشرز، شبه 24س) أو `^DJI` (كاش — الفريمات الصغيرة فيه محصورة بجلسة أمريكا)، الناسداك `NQ=F` أو `^IXIC`/`^NDX`. **للفريمات الصغيرة استخدم الفيوتشرز** (GC/YM/NQ) لاستمرارية الشموع. **وحدة النقطة لكل أداة**: فوركس 0.0001، أزواج الين 0.01، الذهب 0.1$، المؤشرات نقطة كاملة (1.0) — والخانات العشرية للأسعار تتبع الأداة (فوركس 5، ين 3، ذهب 1، مؤشرات 0). التسمية بالعربي: «الذهب»، «الداو جونز»، «الناسداك». سجل الجارتات يعمل بمفتاح (symbol, tf) فقاعدة عدم التكرار مستمرة تلقائيًا لكل أداة.

## 5. الأشكال — حادة ورسمية

- **border-radius: 0–3px لكل شيء** (عدّادات، تاقات، pills، صناديق CTA). ممنوع الزوايا الدائرية الكبيرة.
- الحدود رفيعة 1.5px، التعبئة شفافة أو خفيفة جدًا.
- **شارات الأرقام**: مربع بزوايا **صفر**، أرقام **لاتينية 1 2 3** مائلة (italic) عريضة 900 — ممنوع ١ ٢ ٣ بالشارات.
- العدّاد (n/m): flex بالاتجاه LTR: `<b>١</b><i>/</i><b>٨</b>` عشان ما ينعكس.

## 6. المعجم الكويتي المعتمد (إلزامي — يفوز على أي صياغة ثانية)

| ✅ نقول | ❌ ما نقول |
|---|---|
| **حدد** القمم والقيعان | علّم القمم والقيعان |
| تغيّر **نمط** | تغيّر طابع |
| **خلّ الدرس معاك** | خذ الدرس معك |
| **يبين معاك** الهيكل | يبان لك الهيكل |

+ باقي قواعد `fahad-script-voice`. كلمة الـ ManyChat للدرس: **«ستركجر»**. «لغرض تعليمي» على أي محتوى فيه صفقة/بيانات حقيقية.

## 7. الحركة (الريلز)

- كانفاس 1080×1920 @ 30fps. المدة حسب `skip-rate-under-30` (نسخة الستركجر المعتمدة: ~61s).
- انتقالات المشاهد: crossfade دخول 0.45s / خروج 0.4s + Ken Burns خفيف 1.008→1.028.
- الشموع تنبني تتابعيًا: scaleY من 0.2→1 بـ back-ease، stagger ‏0.07–0.16s.
- الخطوط تنرسم بـ stroke-dashoffset (كلاس `.draw`)، الوسوم تنبثق scale 0→1 back-ease (`.pop`).
- **لحظة الكسر (الذروة)**: فلاش أبيض ≤0.5 opacity لمدة 0.45s + zoom punch جيبي 6% (~1.1s) بمركز نقطة الكسر.
- **أسلوب الشرح متعدد الخطوات (معتمد):** الخطوات المتتابعة على نفس المثال تُشرح على **جارت واحد ثابت** يبقى على الشاشة، والتحديد يتراكم عليه خطوة بخطوة (العناوين والكابشنات تتبدل فوقه بcrossfade، وكل طبقة ماركب تظهر عند خطوتها) — ممنوع تقسيم الخطوات على مشاهد/صفحات بشارتات منفصلة. نمط `ob_chart_steps()` في `reel_ob_build.py`. (قاعدة عدم التكرار تُحسب هنا كاستخدام واحد للجارت.)
- 🔒 **أمر دائم — ريبلاي حي أثناء التحديد:** في الشرح خطوة بخطوة، الشموع نفسها تتكشف تدريجيًا مع القصة (الهبوط ← تحديد الشمعة ← الاندفاع يظهر ← BOS ← الزون يُرسم على فراغ المستقبل ← السعر يرجع يختبره ← الرالي) — نمط `cp1..cp4` في `ob_chart_steps()`.
- 🔒 **أمر دائم — الخطوط تتوقف عند شمعتها:** خط BOS يمتد من آخر قمة إلى **شمعة الكسر فقط**، وخط السيولة إلى **شمعة السحب فقط** — ممنوع مدّ الخطوط لآخر الشارت (الزون فقط يُمدّ لقدام). السهم **بجانب شمعة الكسر بمسافة واضحة** (منتصف الفجوة بعدها، ~0.55 slot): يبدأ من تحت قاعها ورأسه يلامس الخط — ما يلامس جسم الشمعة، والخط يمتد ~0.78 slot بعد الشمعة ليستقبله.
- 🔒 **أمر دائم — النتيجة أولًا:** نتيجة الصفقة/المثال تظهر في **أول ثواني الريل** (بعد البوستر مباشرة أو داخله): لقطة الشارت النهائية بالنتيجة (الكسر واصل الهدف، الحركة كاملة، +نقاط) قبل أي شرح. الهوك = «هذي النتيجة… خلني أوريك كيف تتحدد». بعدها يبدأ الشرح خطوة بخطوة ويرجع لنفس المثال في الذروة.
- ترتيب المشاهد المعتمد (12): بوستر غامق → المشكلة → التعريف → 3 حالات السوق → صاعد → هابط → تحديد القمم/القيعان → لحظة الانقلاب (CHoCH) → BOS vs CHoCH → السوق الحقيقي (الذروة) → اقتباس → CTA («خلّ الدرس معاك»).

## 8. الكاروسيل

1080×1350، نفس الألوان الفاتحة، Tajawal، عدّاد ن/م، «اسحب →»، سلايد اقتباس قابل للمشاركة، CTA بكلمة مفتاحية. سكربتات: `carousel_build.py` + `carousel_render.py` (Playwright بدقة 2x).
- المرجع المحدث (2026-08-02): `carousel3_build.py` + `carousel3_render.py` (كاروسيل «الفجوة السعرية FVG») — غلاف غامق (بوستر) بهوك سؤال، والباقي فاتح، عدّاد وأرقام شارات **لاتينية مائلة LTR**، زوايا حادة 0–3px، هيكل الحفظ: هوك ← المشكلة ← 3 نقاط مرقمة ← السوق الحقيقي ← اقتباس ← CTA بكلمة مفتاحية. (`carousel2_build.py` كاروسيل الريتيست محفوظ كمثال هندسي لكنه **مستبدل** — كرر موضوع الشيت والريل وخالف قاعدة 8c؛ لا يُنشر إلا بطلب صريح.)

## 8c. عدم تكرار المواضيع 🔒

🔒 **أمر دائم (2026-08-02):** ممنوع تكرار المواضيع بين التصاميم — كل تسليم جديد (ريل، كاروسيل، شيت، بوست، PDF) = **درس/موضوع جديد** ما انبنى قبل بأي صيغة. تحويل نفس الدرس لصيغة ثانية (مثلًا شيت ← كاروسيل) لا يصير إلا **بطلب صريح من فهد**. قبل أي تصميم جديد راجع هالسجل، وبعد التسليم حدّثه.
- **مصدر الحقيقة لسجل المواضيع المغطاة:** `content/topics_backlog.json` — كل موضوع status=done (مع تاريخه وصيغته وكلمته) + قائمة covered_before_backlog. راجعه آليًا قبل أي إنتاج؛ وسجل العبارات المحظورة في `content/hooks_log.json`. (تم التخلي عن تكرار القائمة هنا يدويًا — كانت تتقادم.)

## 8a. ثيم التصميم المفرد 🔒

🔒 **أمر دائم (2026-08-01):** أي تصميم يُسلَّم **كصورة واحدة** (شيت نماذج، بوست مفرد، اقتباس، إعلان صورة) يُنفذ **بثيم الهوية الغامق** (نفس نظام البوستر: خلفية radial ‏#0C2029→#08131C→#04090F، سيان #43D4DC للشارات والفواصل، عناوين بتدرج معدني أبيض→فضي مع توهج سيان خفيف). المحتوى **متعدد الصفحات/الشرائح** (كاروسيل، PDF، صفحات الريل الداخلية) يبقى بالثيم الفاتح والغلاف فقط غامق.
- شارتات الثيم الغامق: شمعة صاعدة `#43D4DC`، هابطة `#5E7A88`، الخطوط/الوسوم المحايدة `#D8E5EB`، هالة النص بلون الخلفية `#08131C`، أكسنت تيل `#3AAFC0`، أحمر `#E05656`، شبكة `rgba(255,255,255,0.06)` — جاهزة كخريطة `DK_MAP` + دالة `dk()` في `scripts/sheet_build.py` (تحويل نصي لألوان أي SVG فاتح).

## 8b. شيتات شرح النماذج (صور تعليمية ثابتة) 🔒

🔒 **أمر دائم (2026-08-01):** عند طلب «صور تشرح نماذج» اعتمد نمط **الشيت المزدوج** (مرجع فهد: ورقة "Types of Bullish Orderblock Retest"):
- الصفحة = عنوان علوي كبير + **2–3 صفوف**، كل صف نموذج واحد بعمودين متجاورين:
  1. **رسم تخطيطي خطي** (zigzag مفهومي): خط سعر INK، خطوط نموذج/قناة، صندوق الزون، **دائرة على نقطة الدخول/الريتيست**، سهم استمرار للأعلى/الأسفل، وسوم (BOS، اسم النموذج، PDL…).
  2. 🔒 **جانب الشموع = نافذة حقيقية من السوق** (أمر «اجعل الجارتات واقعية»): `scripts/sheet_scan.py` في sandbox يفحص عدة أدوات/فريمات (ذهب، مؤشرات فيوتشرز، أزواج؛ 15m/30m/1h — الساعة يعطي سنة كاملة) عن النمط الفعلي ويرجع أفضل المرشحين، والاختيار النهائي **بصري** (رندر البدائل وقارن) — الماركب مثبّت على OHLC الحقيقي: BOS حتى شمعة الكسر، خطوط النموذج على رؤوس الفتائل الفعلية، الدائرة على قاع شمعة الريتيست. كل صف بأداة مختلفة إن أمكن + شريحة «الأداة · الفريم · التاريخ». النوافذ تُسجل في السجل. `gen()` يبقى بديلًا أخيرًا فقط إذا ما وُجد نمط حقيقي نظيف.
- **بهوية الحساب لا بستايل الدفتر**: كانفاس كريمي فاتح، خطوط INK `#0F2E3C`، زونات تركواز شفافة (ممنوع الرمادي)، أحمر `#D24B4B` للهبوطي/خطوط النموذج التحذيرية، شموع بألواننا، Tajawal مع هالة كريمية، زوايا حادة 0–3px، لوقو + LIQUIDITY STATE أعلى الصفحة، عدّاد صفحات إذا كانت سلسلة.
- المقاسات: `SIZE="post"` ‏1080×1350 (منشور) أو `SIZE="reel"` ‏1080×1920 (ريل/ستوري) — فلاغ في `sheet_build.py` يضبط ارتفاع الصفحة والشارتات تلقائيًا. وكل شارت شموع بالشيت يسجَّل في سجل عدم التكرار كالمعتاد.

## 9. خط الإنتاج (Pipeline)

```bash
cd .claude/skills/liquidity-reel-production/scripts
./fetch_fonts.sh                 # مرة وحدة — ينزّل Tajawal
pip3 install playwright Pillow imageio-ffmpeg --quiet
python3 reel_build.py            # يولّد reel.html (مشاهد + timeline JS)
python3 reel_sample.py 3 21 53   # عيّنات QA — راجعها بصريًا قبل الرندر الكامل
python3 reel_render.py           # يلتقط الإطارات (Playwright/chromium بـ /opt/pw-browsers) ويجمعها MP4 (libx264 crf19 faststart)
```

- Chromium: `/opt/pw-browsers/chromium-*/chrome-linux/chrome` — لا تسوي `playwright install`.
- الخطوط base64 داخل الـ HTML (تحميل خارجي = رندر بدون خط).
- **QA إلزامي قبل التسليم**: افحص إطارات فعلية — العربي متصل؟ الأرقام مو معكوسة؟ الماركب منطبق على الفتائل؟ مافي تداخل نصوص؟ (قواعد `arabic-video-text`).

## 10. الصوت — خط الإنتاج المعتمد (شغّال منذ 2026-08-01)

الريلز بدون صوت غير مكتمل للنشر (تجربة الصامت: 95 مشاهدة فقط). الخط الشغّال:
1. **المفتاح**: محفوظ محليًا في `.env` بجذر الريبو (**غير مُتتبع** — في .gitignore، ممنوع يُكمّت أبدًا). SDK ‏`elevenlabs` منصّب محليًا + سكِل ElevenLabs الرسمية في `.claude/skills/text-to-speech`. حسابه Free — أصوات المكتبة العربية مقفلة عبر الـAPI، والمعتمد: **صوت `Daniel` ‏(onwK4e9ZLuTAKqWW03F9)** — صوت واحد لكل الفيديوهات إلى أن يستنسخ فهد صوته (يتطلب Starter).
   - **اختيار النموذج (مقاس 2026-08-01):** `eleven_v3` أدق نطقًا للعربي (94.5% مقابل 85.7% بفحص Whisper) لكنه أبطأ ~15% **وإيقاعه غير حتمي** (نفس السطر يطلع 2.4ث مرة و3.0ث مرة) — ما ينضبط بجدول 20.5ث صارم على الأصوات الجاهزة. القاعدة: **`eleven_multilingual_v2` للريلات القصيرة المضبوطة بالثواني، و`eleven_v3` للمحتوى الأطول** — ويُعاد قياسه مع صوت فهد المستنسخ أول ما يتوفر.
   - **التشكيل الكويتي الانتقائي: لـ v3 فقط** (مقاس بالتجربتين): على `eleven_v3` يحسّن النطق فعليًا («اِنْطَق السْتوب» صارت مضبوطة)، لكن على `eleven_multilingual_v2` **يضره** — يدمج الكلمات («تِطِق سْتوبِك» تطلع مشوّهة). القاعدة: v2 = نص عادي بدون تشكيل، v3/الصوت المستنسخ = السطور المشكولة في `scripts/vo_lines_tashkeel.txt` مع وسوم ([curious]/[serious]/[confident]) بلا إفراط. **بدون «…» دائمًا** (تولّد وقفات طويلة).
   - **فلترة الصوت المعتمدة**: `highpass=f=70, afftdn=nf=-28, deesser, acompressor, loudnorm I=-14:TP=-1.2:LRA=9`.
   - **صوت فهد المستنسخ**: غير موجود بحساب المفتاح الحالي (Free، الاستنساخ مقفل) — أول ما يوصل مفتاح الحساب اللي فيه الصوت: نفس السطور المشكولة + قياس إيقاع الصوت الفعلي وإعادة الجدولة عليه قبل الاعتماد.
2. **التوليد داخل sandbox_exec** (شبكة الحاوية تحجب api.elevenlabs.io): سطر لكل مقطع، بدون «…» (تولّد وقفات طويلة)، stability 0.5 / similarity 0.8 / style 0.3.
3. **الجدولة الآلية**: سكربت بايثون يقيس مدة كل مقطع (ffprobe) ويثبّته على توقيت نبضته مع دفع متسلسل (فجوة 0.1ث) وatempo حتى 1.22 (1.28 للسطور الأخيرة) — السطور تُقصَّر إذا فاضت، ما تُسرَّع فوق ذلك.
4. **الماسترينغ**: amix ‏→ compressor ‏→ `loudnorm I=-14:TP=-1.2:LRA=9` ‏→ AAC 128k.
5. **النقل للحاوية عبر جسر vo-bridge**: رفع الماستر على litterbox.catbox.moe (رابط مباشر) ← تحديث `assets/vo/request.json` ‏(url+md5) ودفعه ← ورك فلو `.github/workflows/vo-bridge.yml` يتحقق من md5 ويكمّت الملف بالريبو ← pull ← دمج `ffmpeg -map 0:v -map 1:a -c:v copy`.
6. فحص نهائي: التفريغ العكسي للماستر يطابق السطور بمواقعها قبل الدمج.
7. 🔒 **أفضل-من-N (معيار الإتقان):** v3 غير حتمي نطقًا وإيقاعًا — لكل سطر ولّد 2–3 محاولات، فرّغها عكسيًا بـWhisper، اختر الأعلى دقةً (الأقصر عند التعادل)، وبعدها زامن مشاهد الفيديو على جدول المحاولات الفائزة (الفيديو بارامتري — الصوت هو المرجع). خفف كثافة الكلام حتى يكون التسريع ≤1.08: احذف سطور الانتقال اللي تقولها الشموع بصريًا («بعدين يرد للزون» = صمت درامي قبل «انطق الستوب»). السكربت المرجعي داخل الجلسة (gb3.py pattern): توليد ← فحص ← اختيار ← جدولة ← ماستر ← رفع، كله بعملية خلفية واحدة تطبع النتائج قبل موت الـsandbox، والنداءات القصيرة فقط للمتابعة (مهلة أداة النقل 60ث).

## 11. مصنع المحتوى اليومي 🔒 (أتمتة كاملة — أمر دائم 2026-08-02)

Routine مجدول («مصنع المحتوى اليومي»، كرون يومي 04:00 UTC = 07:00 الكويت) يوقظ هالجلسة لإنتاج **5 وحدات محتوى** بدون تدخل فهد. كل وحدة = تصميم (كاروسيل أو ريل) **+ ملف PDF** خاص بكلمتها (الليد ماغنت اللي يوصل لمن يعلّق).

### بروتوكول التشغيلة (يُنفَّذ حرفيًا كل صباح)
1. **المواضيع**: اقرأ `content/topics_backlog.json` — اسحب أول 5 مواضيع `queued` (تنويع المحاور: لا تاخذ موضوعين من نفس المحور بنفس اليوم إن أمكن — اقفز للي بعده). سدّد أولًا أي `pdf_debts` معلّقة (تحسب من الخمسة).
2. **المزيج**: افتراضيًا 4 كاروسيلات + 1 ريل (حسب `format` المقترح، مع حد أقصى ريل واحد يوميًا بسبب حصة ElevenLabs المجانية ~10k حرف/شهر). إذا فشل جسر الصوت أو نفدت الحصة: حوّل وحدة الريل إلى كاروسيل أو شيت نماذج — **ممنوع نشر ريل بدون VO**.
3. **الكتابة**: كل وحدة بصوت فهد (سكيل `fahad-script-voice` — المعجم قانون، فحص §6 قبل التسليم) وهيكل الحفظ (`carousel-design-pro`): هوك ← المشكلة ← 3 نقاط مرقمة ← السوق الحقيقي ← اقتباس ← CTA بالكلمة المفتاحية.
4. **البناء**: القالب المعياري `scripts/carousel3_build.py` (انسخه لملف جديد وبدّل المحتوى) — غلاف غامق + محتوى فاتح + زوايا حادة + أرقام لاتينية مائلة. الريل بمحرك `reel_sheet_build.py` pattern + قوانين skip-rate (≤22.4ث). الـPDF بمحرك `doc_build.py` + `doc_render.py`: دليل 5–7 صفحات بنفس الهوية (غلاف غامق + صفحات فاتحة).
5. **الشارتات الحقيقية**: نافذة جديدة لكل تصميم من المرشحات الحرة في `scripts/sheet_candidates.json` — تحقق `assert_fresh_real` وسجّل `register_real` في `used_charts.json`. عند انخفاض الحر عن 3: شغّل `sheet_scan.py` في sandbox_exec (هيغزفيلد) واجلب دفعة جديدة عبر بروتوكول النقل (§9).
6. **الجودة**: رندر كل صفحة/إطار وافحصها بصريًا (عربي متصل، لا تداخل، ماركب على الفتائل، «لغرض تعليمي» مع البيانات الحقيقية) — نفس معايير §الجودة.
7. **التسليم**: أرسل الملفات لفهد (SendUserFile — proactive): لكل وحدة صور الكاروسيل + zip + الـPDF، مع كابشن مقترح وكلمة ManyChat. ملخص واحد بآخر الرسالة: جدول الوحدات الخمس بكلماتها.
8. **التحديث والالتزام**: حدّث `topics_backlog.json` ‏(status=done + التاريخ)، سجل 8c في هذا الملف، `used_charts.json`، وانسخ سكربتات البناء الجديدة لـ`scripts/` — ثم commit + push (الأتمتة تعيش بالريبو، ليس بالحاوية).
9. **التجديد**: إذا بقي بالبنك أقل من 10 مواضيع `queued` — ولّد 15 موضوعًا جديدًا (محاور متنوعة، غير مكررة مع done ومع سجل 8c) وأضفها.
10. **الفشل الجزئي**: أي وحدة تفشل لا توقف الباقي — سلّم اللي نجح، واذكر الفاشل وسببه بالملخص، وأعده لأول الطابور.

### حدود مقرّة
- الريلز محدودة بحصة الصوت المجانية — أول ما توصل حصة الشهر حدها، باقي أيام الشهر كاروسيلات وشيتات فقط (يُذكر بالملخص اليومي).
- صوت «فهد» المستنسخ ينتظر مفتاح الحساب الصح — أول ما يوصل، بدّل Daniel في خط §10 وارفع جودة الريلات.
- **تشغيلة بدون أدوات خارجية (MCP):** التشغيلات المجدولة قد تصحى بدون sandbox_exec (لا مسح Yahoo جديد ولا جسر صوت). عندها: الريل يتحول كاروسيل/شيت، والنوافذ الحقيقية تُسحب من مخزون `sheet_candidates.json` الحر فقط. إذا نفد المخزون: ابنِ الوحدة **بدون سلايد السوق الحقيقي** (تخطيطيات فقط) واذكر بالملخص أن سلايد التطبيق مؤجل، وبأول جلسة يتوفر فيها الساندبوكس شغّل `sheet_scan.py` وعبّئ المخزون (20+ نافذة) وسدّد السلايدات المؤجلة.

## 12. صوت التصاميم = مؤثرات فقط 🔒 (أمر دائم 2026-08-03)

🔒 **أمر من فهد:** الفويس أوفر بكل التصاميم الجديدة = **مؤثرات صوتية فقط، بدون أي كلام منطوق**. خط TTS في §10 يتجمد (يُستخدم فقط لو طلب فهد كلامًا صراحةً) — وهذا يلغي قيد حصة ElevenLabs عن الريلات نهائيًا.

- **المكتبة المحلية** (أوفلاين، مولدة بتوليف ffmpeg، بالريبو): `assets/sfx/` —
  `whoosh.wav` (0.7ث — انتقالات/رسم خطوط) · `pop.wav` (وسوم تنبثق) · `tick.wav` (تكشّف الشموع، كل 2–4 شموع مو كل شمعة) · `riser.wav` (1.3ث — شد قبل الذروة) · `impact.wav` (لحظة الكسر/الفلاش) · `success.wav` (النتيجة/الوصول للهدف).
- **المزامنة**: كل مؤثر مثبّت على لحظة حدثه بالمشهد (بداية رسم الخط، انبثاق الوسم، فريم الفلاش) بدقة الجدول الزمني للبناء — نفس فلسفة «الصوت مرجع» لكن هنا الفيديو هو المرجع والمؤثرات تتبعه.
- **الماستر**: amix للطبقات ← `loudnorm I=-16:TP=-1.5:LRA=11` (مؤثرات بدون كلام = مستوى أخفض شوي من -14) ← AAC 128k ← دمج `-map 0:v -map 1:a -c:v copy`.
- **الكثافة**: مؤثر واحد لكل حدث بصري مهم — ممنوع رشّ المؤثرات على كل فريم؛ الصمت بين المؤثرات جزء من الإيقاع. الذروة: riser ← impact ← success بهذا الترتيب.
- قاعدة skip-rate «VO إلزامي» تُقرأ الآن: **طبقة صوت مؤثرات إلزامية** — ممنوع ريل صامت تمامًا.
- بالمصنع اليومي (§11): الريلات ما عادت مقيدة بحصة صوت — المزيج يتبع اقتراح `format` بالبنك، وقيد «ممنوع ريل بدون VO» يصير «ممنوع ريل بدون طبقة مؤثرات».
- 🔒 **توزيع الفئات اليومي (أمر فهد 2026-08-03):** الوحدات الخمس كل يوم تغطي الفئات الأربع إلزاميًا: **2 فنية + 1 نفسية + 1 أساسية + 1 مالية** (حقل `cat` في البنك). التجديد يحافظ على 5+ مواضيع queued بكل فئة.
- 🔒 **كل موضوع = كاروسيل + ريل + PDF (أمر فهد 2026-08-03):** الوحدة اليومية الواحدة تشمل الثلاثة مع بعض — كاروسيل 8 صفحات، وريل ≤22.4ث بمؤثرات فقط (§12) يشتق قصته من نفس سلايدات الكاروسيل (نفس الوحدة = يجوز مشاركة نوافذها الحقيقية)، وPDF بكلمة الموضوع. المواضيع اللي طلعت قبل هالقاعدة بدون فيديو مسجلة في `media_debts` بالبنك وتُسدد بأول التشغيلات (تحسب من الخمسة). لو ضاق وقت التشغيلة: كمّل وحدات كاملة أول، ورحّل الناقص لأول طابور بكرة — ممنوع تسليم وحدة ناقصة صيغة بدون ذكرها بالملخص.
- 🔒 **تحديث (أمر فهد 2026-08-03 لاحقًا — يلغي بند «كاروسيل + ريل + PDF» أعلاه):** الوحدة اليومية = **كاروسيل + دليل PDF بكلمته فقط، بدون فيديو**. الريلات تُنتج فقط بطلب صريح من فهد (وعندها بمؤثرات فقط حسب §12). ديون الفيديو السابقة ملغاة.
- 🔒 **التركيبة النهائية (أمر فهد 2026-08-03 — تلغي البندين السابقين):** كل يوم **5 وحدات: 3 كاروسيلات + 2 ريلات**، وكل وحدة (كاروسيل أو ريل) معها **دليل PDF بكلمتها**. الريلات بمؤثرات صوتية فقط (§12) و≤22.4ث، وتُعطى أولوية للمواضيع الفنية. توزيع الفئات الأربع على الخمسة كما هو (2 فنية + 1 نفسية + 1 أساسية + 1 مالية).
- 🔒 **ايجنت صانع المحتوى (أمر فهد 2026-08-03):** كتابة نصوص الوحدات وتوليد المواضيع الجديدة تتم عبر الايجنت `content-maker` (ملفه في `.claude/agents/content-maker.md`، يُستدعى بأداة Agent بـ`subagent_type: "content-maker"`). واجباته: يقرأ البنك وسجل 8c و`content/hooks_log.json` قبل الكتابة، **ممنوع يكرر أي فكرة أو هوك أو اقتباس أو قالبه البلاغي**، يعيد الصياغة من الصفر (هوكين بديلين لكل وحدة ويختار الأقوى)، ويحدّث `hooks_log.json` بعد كل تشغيلة. التشغيلة اليومية تسلّمه اختيار المواضيع وكتابة النصوص، ويرجع للتشغيلة الرئيسية البناء والرندر والفحص والتسليم.
